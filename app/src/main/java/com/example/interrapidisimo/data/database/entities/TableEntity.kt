package com.example.interrapidisimo.data.database.entities

/*@Entity(tableName = TableEntity.TABLE_NAME)

class TableEntity(
    @PrimaryKey(autoGenerate = true) val id: Int,
    @ColumnInfo(name = NAME) val name: String,
    @ColumnInfo(name = PRIMARY_KEY) val primaryKey: String,
    @ColumnInfo(name = BATCH_SIZE) val batchSize: String,
    @ColumnInfo(name = FIELD) val field: String,
    @ColumnInfo(name = UPDATE_DATE) val updateDate: String,
) {
    companion object {
        const val TABLE_NAME = "TABLES_NAMES"
        const val NAME = "NAME"
        const val PRIMARY_KEY = "PRIMARY_KEY"
        const val BATCH_SIZE = "BATCH_SIZE"
        const val FIELD = "FIELD"
        const val UPDATE_DATE = "UPDATE_DATE"
    }
}

 */


